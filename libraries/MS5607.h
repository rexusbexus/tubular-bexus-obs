#ifndef MS5607_h
#define MS5607_h

#include <Arduino.h>

class MS5607
{
reset_sequence()
    protected:
        
    Public:
        uint16_t PROMbyte;
        uint32_t uADCvalue;
        uint16_t = convertD1;
        uint16_t = convertD2;
        reset_sequence();
        convertionD1(int i);
        convertionD2(int i);
        PROMread(uint8_t i);
        readADC();
    
    
};

#endif
